To run use docker and use command to start "docker compose up -d --build", after this actions edit config file from data/config.json
Change groups to send messages in src/emission_loop.py
Many parameters are hardcoded here
To get api key for https://stalcraft-status.ru/ search on the same site

Для запуска проекта воспользуйтесь командой: "docker compose up -d --build"
Много параметров захардкожено, за проектом слежу, контрибъючу
